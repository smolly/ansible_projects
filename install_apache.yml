---

- hosts: 10.46.11.65
  become: true
  tasks:

  - name: install apache and php package
    package:
      name: 
        - "{{ apache_package }}"
        - "{{ php_package }}"
      state: latest
      update_cache: no
